<%= form_for @article, url: {controller: "content", action: "merge", id: @article.id}, html: {:method => 'post', class: "nifty_form"} do |f| %>
  <h2>Merge Articles</h2>
  <p>
    Article ID: <%= text_field_tag :merge_with, params[:merge_with] %>
  </p>
  <p>
  <%= f.submit "Merge" %>
<% end %>

